---
- name: Install Nginx on web servers
  hosts: localhost
  become: yes  # Use sudo to execute tasks
  tasks:
    - name: Ensure Nginx is installed
      apt:
        name: nginx
        state: present
